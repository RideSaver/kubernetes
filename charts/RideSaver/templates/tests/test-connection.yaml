apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "RideSaver.fullname" . }}-test-connection"
  labels:
    {{- include "RideSaver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "RideSaver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
