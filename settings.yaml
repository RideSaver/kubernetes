environments:
  default:
    values:
      - ingress:
          enabled: true
          hosts:
            - host: ride-saver.online
              paths:
                - path: /
                  pathType: ImplementationSpecific
