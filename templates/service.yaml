apiVersion: v1
kind: Service
metadata:
  name: {{ include "RideSaver.fullname" . }}
  labels:
    {{- include "RideSaver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "RideSaver.selectorLabels" . | nindent 4 }}
